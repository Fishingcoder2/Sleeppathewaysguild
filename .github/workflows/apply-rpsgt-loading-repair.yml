name: Apply Maria Sosa bookstore feature

on:
  push:
    paths:
      - '.github/workflows/apply-rpsgt-loading-repair.yml'

permissions:
  contents: write

jobs:
  maria-sosa-feature:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Add Maria Sosa featured-author shelf
        run: python scripts/one_time_maria_sosa_books.py

      - name: Run SEO audit
        run: python scripts/seo_audit.py --site-url https://sleeppathwaysguild.com

      - name: Run link audit
        run: python scripts/link_audit.py --site-url https://sleeppathwaysguild.com

      - name: Restore helper and remove one-time script
        shell: bash
        run: |
          cat > .github/workflows/apply-rpsgt-loading-repair.yml <<'EOF'
          name: Apply RPSGT loading repair

          on:
            workflow_dispatch:

          permissions:
            contents: read

          jobs:
            disabled:
              if: false
              runs-on: ubuntu-latest
              steps:
                - run: echo 'Superseded by the verified browser loader deployment.'
          EOF
          rm -f scripts/one_time_maria_sosa_books.py

      - name: Commit verified author feature
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m 'feat: add Maria Sosa sleep books to bookstore'
          git push
