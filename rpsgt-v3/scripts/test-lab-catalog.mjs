import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import vm from 'node:vm';import {dirname,join} from 'node:path';import {fileURLToPath} from 'node:url';
const here=dirname(fileURLToPath(import.meta.url));const root=join(here,'..');const source=await readFile(join(root,'core','lab-catalog-engine.js'),'utf8');const catalog=JSON.parse(await readFile(join(root,'data','labs','catalog.json'),'utf8'));
const context={globalThis:{},JSON,Map,Set,Object,Array,Number,String,Boolean,Math};vm.createContext(context);vm.runInContext(source,context,{filename:'lab-catalog-engine.js'});const engine=context.globalThis.RPSGTLabCatalogEngine;
const validation=engine.validateCatalog(catalog);assert.equal(validation.valid,true);assert.equal(validation.count,10);assert.equal(new Set(catalog.labs.map(lab=>lab.id)).size,10);assert.equal(catalog.meta.individualLabParityComplete,false);
const linked=catalog.labs.filter(lab=>lab.status==='legacy-linked');assert.equal(linked.length,1);assert.equal(linked[0].id,'ekg');assert.equal(linked[0].legacyHref,'../ekg.2026.html');
const ready=catalog.labs.filter(lab=>lab.status==='v3-ready');assert.deepEqual(ready.map(lab=>lab.id).sort(),['hookup','instrumentation','math-coach','pap','pediatric','respiratory','scoring','troubleshooting']);assert.equal(ready.find(lab=>lab.id==='hookup').plannedRoute,'lab-hookup.html');assert.equal(ready.find(lab=>lab.id==='scoring').plannedRoute,'lab-scoring.html');assert.equal(ready.find(lab=>lab.id==='respiratory').plannedRoute,'lab-respiratory.html');assert.equal(ready.find(lab=>lab.id==='instrumentation').plannedRoute,'lab-instrumentation.html');assert.equal(ready.find(lab=>lab.id==='pap').plannedRoute,'lab-pap.html');assert.equal(ready.find(lab=>lab.id==='pediatric').plannedRoute,'lab-pediatric.html');assert.equal(ready.find(lab=>lab.id==='troubleshooting').plannedRoute,'lab-troubleshooting.html');assert.equal(ready.find(lab=>lab.id==='math-coach').plannedRoute,'lab-math-coach.html');
const sourceProgress={completed:['hookup'],started:{pap:{startedAt:'2026-08-02'}},catalogIndex:1};const before=JSON.stringify(sourceProgress);const report=engine.summarize(catalog,sourceProgress);assert.equal(JSON.stringify(sourceProgress),before);assert.equal(report.counts.total,10);assert.equal(report.counts.completed,1);assert.equal(report.counts.started,1);assert.equal(report.counts.v3Ready,8);assert.equal(report.last.id,'ekg');assert.equal(report.rows.find(row=>row.id==='hookup').completed,true);assert.equal(report.rows.find(row=>row.id==='pap').started,true);
const objectProgress=engine.normalizeProgress({scoring:{completed:true},respiratory:{completed:true},instrumentation:{completed:true},pap:{completed:true},pediatric:{completed:true},troubleshooting:{completed:true}});assert.ok(objectProgress.completed.includes('scoring'));assert.ok(objectProgress.completed.includes('respiratory'));assert.ok(objectProgress.completed.includes('instrumentation'));assert.ok(objectProgress.completed.includes('pap'));assert.ok(objectProgress.completed.includes('pediatric'));assert.ok(objectProgress.completed.includes('troubleshooting'));
console.log('Laboratory catalog, stable identifiers, conservative links, eight v3-ready routes, and progress-normalization contracts passed.');
