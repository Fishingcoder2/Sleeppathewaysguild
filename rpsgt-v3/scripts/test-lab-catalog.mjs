import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import vm from 'node:vm';import {dirname,join} from 'node:path';import {fileURLToPath} from 'node:url';
const here=dirname(fileURLToPath(import.meta.url));const root=join(here,'..');const source=await readFile(join(root,'core','lab-catalog-engine.js'),'utf8');const catalog=JSON.parse(await readFile(join(root,'data','labs','catalog.json'),'utf8'));
const context={globalThis:{},JSON,Map,Set,Object,Array,Number,String,Boolean,Math};vm.createContext(context);vm.runInContext(source,context,{filename:'lab-catalog-engine.js'});const engine=context.globalThis.RPSGTLabCatalogEngine;
const validation=engine.validateCatalog(catalog);assert.equal(validation.valid,true);assert.equal(validation.count,10);assert.equal(new Set(catalog.labs.map(lab=>lab.id)).size,10);assert.equal(catalog.meta.individualLabParityComplete,true);assert.equal(catalog.meta.version,10);
const linked=catalog.labs.filter(lab=>lab.status==='legacy-linked');assert.equal(linked.length,0,'No v3 catalog route should depend on a legacy laboratory page.');
const ready=catalog.labs.filter(lab=>lab.status==='v3-ready');assert.deepEqual(ready.map(lab=>lab.id).sort(),['daytime-testing','ekg','hookup','instrumentation','math-coach','pap','pediatric','respiratory','scoring','troubleshooting']);assert.equal(ready.length,10);assert.equal(ready.find(lab=>lab.id==='hookup').plannedRoute,'lab-hookup.html');assert.equal(ready.find(lab=>lab.id==='ekg').plannedRoute,'lab-ekg.html');assert.equal(ready.find(lab=>lab.id==='ekg').legacyHref,null);assert.equal(ready.find(lab=>lab.id==='scoring').plannedRoute,'lab-scoring.html');assert.equal(ready.find(lab=>lab.id==='respiratory').plannedRoute,'lab-respiratory.html');assert.equal(ready.find(lab=>lab.id==='instrumentation').plannedRoute,'lab-instrumentation.html');assert.equal(ready.find(lab=>lab.id==='pap').plannedRoute,'lab-pap.html');assert.equal(ready.find(lab=>lab.id==='pediatric').plannedRoute,'lab-pediatric.html');assert.equal(ready.find(lab=>lab.id==='daytime-testing').plannedRoute,'lab-daytime-testing.html');assert.equal(ready.find(lab=>lab.id==='troubleshooting').plannedRoute,'lab-troubleshooting.html');assert.equal(ready.find(lab=>lab.id==='math-coach').plannedRoute,'lab-math-coach.html');
const sourceProgress={completed:['hookup'],started:{pap:{startedAt:'2026-08-02'}},catalogIndex:1};const before=JSON.stringify(sourceProgress);const report=engine.summarize(catalog,sourceProgress);assert.equal(JSON.stringify(sourceProgress),before);assert.equal(report.counts.total,10);assert.equal(report.counts.completed,1);assert.equal(report.counts.started,1);assert.equal(report.counts.v3Ready,10);assert.equal(report.last.id,'ekg');assert.equal(report.rows.find(row=>row.id==='hookup').completed,true);assert.equal(report.rows.find(row=>row.id==='pap').started,true);
const objectProgress=engine.normalizeProgress({ekg:{completed:true},scoring:{completed:true},respiratory:{completed:true},instrumentation:{completed:true},pap:{completed:true},pediatric:{completed:true},'daytime-testing':{completed:true},troubleshooting:{completed:true}});assert.ok(objectProgress.completed.includes('ekg'));assert.ok(objectProgress.completed.includes('scoring'));assert.ok(objectProgress.completed.includes('respiratory'));assert.ok(objectProgress.completed.includes('instrumentation'));assert.ok(objectProgress.completed.includes('pap'));assert.ok(objectProgress.completed.includes('pediatric'));assert.ok(objectProgress.completed.includes('daytime-testing'));assert.ok(objectProgress.completed.includes('troubleshooting'));
console.log('Laboratory catalog, stable identifiers, ten native v3-ready routes, complete automated parity, and progress-normalization contracts passed.');
