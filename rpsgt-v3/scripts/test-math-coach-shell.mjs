import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';import {execFileSync} from 'node:child_process';import {dirname,join} from 'node:path';import {fileURLToPath} from 'node:url';
const here=dirname(fileURLToPath(import.meta.url));const root=join(here,'..');
const files=['core/math-coach-engine.js','core/math-coach-view-base.js','core/math-coach-view-practice.js','core/math-coach.js','core/practice-calculator.js'];for(const file of files)execFileSync(process.execPath,['--check',join(root,file)]);
const [html,controller,practiceView,redirect,manifest,css,modalCss,calculatorSource,calculatorCss]=await Promise.all([readFile(join(root,'math-coach.html'),'utf8'),readFile(join(root,'core','math-coach.js'),'utf8'),readFile(join(root,'core','math-coach-view-practice.js'),'utf8'),readFile(join(root,'lab-math-coach.html'),'utf8'),readFile(join(root,'data','math-coach','manifest.json'),'utf8').then(JSON.parse),readFile(join(root,'assets','math-coach.css'),'utf8'),readFile(join(root,'assets','math-question-modal.css'),'utf8'),readFile(join(root,'core','practice-calculator.js'),'utf8'),readFile(join(root,'assets','practice-calculator.css'),'utf8')]);
for(const selector of ['data-math-summary','data-math-catalog','data-math-workspace','data-math-award-overlay','data-study-calculator','data-calculator-display'])assert.match(html,new RegExp(selector));
for(const script of ['core/storage.js','core/math-coach-engine.js','core/math-coach-view-base.js','core/math-coach-view-practice.js','core/math-coach.js','core/practice-calculator.js'])assert.match(html,new RegExp(script.replaceAll('/','\\/')));
assert.match(html,/assets\/math-question-modal\.css/);
assert.ok(html.indexOf('math-coach-engine.js')<html.indexOf('math-coach-view-base.js')&&html.indexOf('math-coach-view-base.js')<html.indexOf('math-coach-view-practice.js')&&html.indexOf('math-coach-view-practice.js')<html.indexOf('core/math-coach.js'));
for(const phrase of ['Learn the formula → demonstrate mastery','not a Skills Lab simulation','curated skill','Open study calculator','temporary scratch work'])assert.match(html,new RegExp(phrase,'i'));
for(const token of ['data/math-coach/manifest.json','skillFiles','RPSGTStorage.save','recordMastery','seenCeremonyIds','pinWhiteboard','setWhiteboardVisibility','data-math-continue-mastery','guidedQuestionId'])assert.match(controller,new RegExp(token.replaceAll('/','\\/')));
for(const token of ['math-question-modal-grid','math-question-toolrail','math-modal-calculator','data-math-whiteboard','data-math-whiteboard-pin','data-math-whiteboard-question','This board stays open while you retry','RPSGTStudyCalculator.init','guidedQuestionId'])assert.ok(practiceView.includes(token),`Math Coach practice view is missing ${token}.`);
for(const token of ['body.math-question-modal-open','.math-workspace[data-question-modal="true"]','.math-question-modal-grid','.math-question-toolrail','.math-whiteboard','.math-whiteboard.pinned','position:fixed','min-height:100dvh'])assert.ok(modalCss.includes(token),`Math question modal CSS is missing ${token}.`);
assert.doesNotMatch(controller,/setTimeout\s*\(/,'Math Coach help must not disappear on a timer.');
assert.doesNotMatch(controller,/localStorage\.(?:setItem|removeItem|clear)/);assert.doesNotMatch(calculatorSource,/localStorage|sessionStorage|RPSGTStorage|fetch\(/);assert.match(calculatorSource,/RPSGTStudyCalculator/);assert.match(calculatorCss,/\.math-calculator-tool/);assert.match(redirect,/url=math-coach\.html/);assert.equal(manifest.skillFiles.length,12);assert.match(css,/prefers-reduced-motion/);assert.match(modalCss,/prefers-reduced-motion/);assert.match(html,/aria-labelledby="math-award-title"/);
console.log('Standalone expanded Math Coach shell, question modal, shared calculator, persistent pinned whiteboard help, v3-only storage, staged flow, and award accessibility contracts passed.');
