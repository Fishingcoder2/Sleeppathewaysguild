import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';import {dirname,join} from 'node:path';import {fileURLToPath} from 'node:url';
const here=dirname(fileURLToPath(import.meta.url)),root=join(here,'..');const [html,js,catalog]=await Promise.all([readFile(join(root,'lab-troubleshooting.html'),'utf8'),readFile(join(root,'core','lab-troubleshooting.js'),'utf8'),readFile(join(root,'data','labs','catalog.json'),'utf8').then(JSON.parse)]);
for(const selector of ['data-troubleshooting-start','data-troubleshooting-summary','data-troubleshooting-stations','data-troubleshooting-workspace'])assert.ok(html.includes(selector),`Troubleshooting page is missing ${selector}.`);
for(const script of ['core/storage.js','core/troubleshooting-lab-engine.js','core/lab-troubleshooting.js'])assert.ok(html.includes(script),`Troubleshooting page does not load ${script}.`);assert.ok(html.indexOf('core/troubleshooting-lab-engine.js')<html.indexOf('core/lab-troubleshooting.js'),'Troubleshooting engine must load before its controller.');
for(const token of ['data/question-bank/d2b.json','data/question-bank/d2c.json','data/question-bank/d3c.json','data/troubleshooting/guided-stations.json','eligibleQuestions','selectQuestions','gradeSession','setStation','applySession','storage.save'])assert.ok(js.includes(token),`Troubleshooting controller is missing ${token}.`);
assert.equal(/localStorage\.(?:setItem|removeItem|clear)/.test(js),false,'Troubleshooting controller must write only through versioned storage.');
assert.ok(html.includes('Study → Apply → Recap')&&html.includes('Activity earns completion')&&html.includes('Wrong Apply answers stay on the same station until corrected'),'Troubleshooting page must expose the guided mastery contract.');
assert.ok(js.includes('AI-generated teaching schematic · Not a patient recording'),'Troubleshooting schematics require the synthetic teaching disclosure.');
assert.ok(html.includes('not independent diagnosis, emergency medicine, or equipment repair')&&html.includes('Complete all seven guided troubleshooting stations')&&html.includes('80% or higher'),'Troubleshooting scope and completion boundaries must remain visible.');
assert.equal(html.includes('Study checklist'),false);assert.equal(html.includes('Mark a station only after reviewing'),false);
const lab=catalog.labs.find(item=>item.id==='troubleshooting');assert.ok(lab&&lab.status==='v3-ready'&&lab.contentStatus==='interactive-rich'&&lab.plannedRoute==='lab-troubleshooting.html','The catalog must route the guided interactive Troubleshooting lab.');
console.log('Troubleshooting page, guided mastery shell, scope boundary, storage isolation, script order, and catalog route contracts passed.');
