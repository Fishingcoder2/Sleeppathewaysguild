import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const here=path.dirname(fileURLToPath(import.meta.url));
const root=path.resolve(here,'..');
const read=relative=>fs.readFileSync(path.join(root,relative),'utf8');
const study=read('study.html');
const practice=read('practice.html');
const completion=read('core/guided-study-completion.js');
const actions=read('core/guided-study-question-actions.js');
const resources=read('core/study-resource-catalog.js');
const storageGuard=read('core/guided-study-storage-guard.js');
const coachSafety=read('core/guided-study-coach-safety.js');
const coachEngine=read('core/coach-bob-engine.js');
const guidedCoach=read('core/guided-study-coach.js');

assert.match(study,/assets\/guided-study-completion\.css/);
assert.match(study,/core\/study-resource-catalog\.js/);
assert.match(study,/core\/coach-bob-engine\.js/);
assert.match(study,/core\/guided-study-coach-safety\.js/);
assert.match(study,/core\/guided-study-completion\.js/);
assert.match(study,/core\/guided-study-storage-guard\.js/);
assert.match(study,/core\/guided-study-coach\.js/);
assert.ok(study.indexOf('coach-bob-engine.js')<study.indexOf('core/study.js'),'Coach Bob engine must be available before the controller renders checkpoints');
assert.ok(study.indexOf('guided-study-completion.js')<study.indexOf('core/study.js'),'completion layer must mount before the controller starts');
assert.ok(study.indexOf('guided-study-storage-guard.js')<study.indexOf('core/study.js'),'storage guard must mount before the controller starts');
assert.ok(study.indexOf('core/study.js')<study.indexOf('guided-study-coach.js'),'event-driven Coach Bob adapter must attach after the checkpoint controller');
assert.match(practice,/core\/practice-prefill\.js/);
assert.match(completion,/Retake with five new questions/);
assert.match(completion,/Begin the next domain/);
assert.match(completion,/Review missed questions/);
assert.match(completion,/seenCeremonyIds/);
assert.match(storageGuard,/data-trail-mark/);
assert.match(storageGuard,/data-checkpoint-score/);

assert.doesNotMatch(coachSafety,/MutationObserver/,'Coach Bob safety utility must not watch or rewrite the DOM');
assert.match(coachSafety,/safePreAnswer/);
assert.match(coachSafety,/containsAnswer/);
assert.doesNotMatch(actions,/enhanceCoach/,'question-actions observer must not generate or rewrite Coach Bob content');
assert.doesNotMatch(actions,/coach-question-panel/,'question-actions observer must stay out of Coach Bob panels');
assert.match(actions,/titlesForQuestion\(question\)/);
assert.match(actions,/MutationObserver\(enhance\)/);
assert.match(actions,/observer\.observe\(checkpointHost,\{childList:true,subtree:true\}\)/);

assert.match(coachEngine,/PATIENT FIRST/);
assert.match(coachEngine,/SIGNAL FAMILY/);
assert.match(coachEngine,/CROSS-CHECK/);
assert.match(coachEngine,/PROOF CLUE/);
assert.match(coachEngine,/answerLeakFree/);
assert.match(coachEngine,/coachBobPreAnswer/);
assert.match(coachEngine,/coachBobCorrect/);
assert.match(coachEngine,/coachBobIncorrect/);
assert.match(coachEngine,/whyTricky/);
assert.match(coachEngine,/examTrap/);
assert.match(coachEngine,/practiceConnection/);
assert.match(coachEngine,/nextAction/);

assert.doesNotMatch(guidedCoach,/MutationObserver/,'Guided Study Coach Bob adapter must be event-driven, not observer-driven');
assert.match(guidedCoach,/data-coach-toggle/);
assert.match(guidedCoach,/data-checkpoint-score/);
assert.match(guidedCoach,/resolveQuestion\(question\)/);
assert.match(guidedCoach,/checkpointHistory/);
assert.match(guidedCoach,/Reasoning Compass/);
assert.match(guidedCoach,/Verified study resources/);
assert.match(guidedCoach,/Repeat pattern/);
assert.match(resources,/topicFamilyFiles/);
assert.match(resources,/titlesForQuestion/);

console.log('Guided Study completion shell contract passed.');
