import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';import {dirname,join} from 'node:path';import {fileURLToPath} from 'node:url';
const here=dirname(fileURLToPath(import.meta.url)),root=join(here,'..');const [html,controller,catalog]=await Promise.all([readFile(join(root,'lab-daytime-testing.html'),'utf8'),readFile(join(root,'core','lab-daytime-testing.js'),'utf8'),readFile(join(root,'data','labs','catalog.json'),'utf8').then(JSON.parse)]);
for(const selector of ['data-daytime-start','data-daytime-summary','data-daytime-stations','data-daytime-workspace','data-daytime-open-station'])assert.ok(html.includes(selector),`MSLT/MWT page is missing ${selector}.`);
for(const script of ['core/storage.js','core/daytime-testing-lab-engine.js','core/lab-daytime-testing.js'])assert.ok(html.includes(script),`MSLT/MWT page does not load ${script}.`);assert.ok(html.indexOf('core/daytime-testing-lab-engine.js')<html.indexOf('core/lab-daytime-testing.js'));
for(const token of ['data/question-bank/d1a.json','data/question-bank/d2c.json','data/question-bank/d3a.json','data/daytime-testing/guided-stations.json','eligibleQuestions','selectQuestions','gradeSession','setStation','applySession'])assert.ok(controller.includes(token),`MSLT/MWT controller is missing ${token}.`);
assert.ok(controller.includes('storage.save')||controller.includes('RPSGTStorage.save'));assert.equal(/localStorage\.(?:setItem|removeItem|clear)/.test(controller),false);for(const forbidden of ['saved.practice','saved.readiness','saved.mock','saved.guidedTrail','saved.review'])assert.equal(controller.includes(forbidden),false);
assert.ok(html.includes('Study → Apply → Recap')&&html.includes('Activity earns completion')&&html.includes('Wrong Apply answers stay on the same station until corrected'));assert.ok(html.includes('AI-generated teaching schematic · Not a patient recording'));assert.ok(html.includes('evidence is insufficient to recommend a pediatric MWT protocol'));assert.ok(html.includes('do not reproduce the AASM protocol boxes'));assert.ok(html.includes('80% or higher'));assert.equal(html.includes('Study checklist'),false);assert.equal(html.includes('Mark a station only after reviewing'),false);
const lab=catalog.labs.find(item=>item.id==='daytime-testing');assert.ok(lab&&lab.status==='v3-ready'&&lab.contentStatus==='interactive-rich'&&lab.plannedRoute==='lab-daytime-testing.html');
console.log('MSLT/MWT page, guided mastery shell, protocol boundary, pediatric MWT limitation, storage isolation, script order, and catalog route contracts passed.');