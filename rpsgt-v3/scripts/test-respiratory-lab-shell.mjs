import {readFile} from 'node:fs/promises';import {dirname,join} from 'node:path';import {fileURLToPath} from 'node:url';
const here=dirname(fileURLToPath(import.meta.url));const root=join(here,'..');const [html,js,css,catalog]=await Promise.all([readFile(join(root,'lab-respiratory.html'),'utf8'),readFile(join(root,'core','lab-respiratory.js'),'utf8'),readFile(join(root,'assets','respiratory.css'),'utf8'),readFile(join(root,'data','labs','catalog.json'),'utf8').then(JSON.parse)]);
for(const selector of ['data-respiratory-start','data-respiratory-summary','data-respiratory-patterns','data-respiratory-pattern-detail','data-respiratory-stations','data-respiratory-workspace']) if(!html.includes(selector)) throw new Error(`Respiratory page is missing ${selector}.`);
for(const script of ['core/storage.js','core/respiratory-lab-engine.js','core/lab-respiratory.js']) if(!html.includes(script)) throw new Error(`Respiratory page does not load ${script}.`);
if(html.indexOf('core/respiratory-lab-engine.js')>html.indexOf('core/lab-respiratory.js')) throw new Error('Respiratory engine must load before its controller.');
for(const token of ['data/question-bank/d2a.json','data/question-bank/d2b.json','data/question-bank/d3b.json','eligibleQuestions','selectQuestions','gradeSession','setStation','applySession','RPSGTStorage.save','patternSvg','wavePath','data-respiratory-pattern','form.elements.namedItem']) if(!js.includes(token)) throw new Error(`Respiratory controller is missing ${token}.`);
if(js.includes('CSS.escape')) throw new Error('Respiratory checkpoint should not depend on CSS.escape for answer retrieval.');
if(/localStorage\.(?:setItem|removeItem|clear)/.test(js)) throw new Error('Respiratory controller must write only through versioned RPSGT storage.');
if(!html.includes('original teaching schematics')||!html.includes('all seven respiratory stations')||!html.includes('80% or higher')) throw new Error('Respiratory evidence and completion boundaries must be visible.');
if(!html.includes('Tap the whole card')||!js.includes('stationHost.addEventListener(\'click\'')||!js.includes('<button class="respiratory-station')) throw new Error('Respiratory station controls are not implemented as reliable full-card buttons.');
for(const visualToken of ['trace-event-window','trace-line airflow','trace-line thorax','trace-line abdomen','trace-line oxygen','delayed O₂ drop']) if(!js.includes(visualToken)) throw new Error(`Respiratory teaching trace is missing ${visualToken}.`);
for(const interactionToken of ['touch-action:manipulation','min-height:68px','min-height:88px','min-height:48px']) if(!css.includes(interactionToken)) throw new Error(`Respiratory touch-target styling is missing ${interactionToken}.`);
if(!css.includes('.respiratory-pattern-button.active')||!css.includes('.respiratory-option:has(input:checked)')) throw new Error('Respiratory selected-state styling is incomplete.');
const lab=catalog.labs.find(item=>item.id==='respiratory');if(!lab||lab.status!=='v3-ready'||lab.plannedRoute!=='lab-respiratory.html') throw new Error('The laboratory catalog does not route the v3-ready Respiratory lab.');
console.log('Respiratory page, seven-pattern visual comparison, full-card station controls, large checkpoint tap targets, evidence boundary, storage isolation, script order, and catalog route contracts passed.');
