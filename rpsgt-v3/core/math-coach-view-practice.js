(function(root){
  'use strict';
  const app=root.RPSGTMathCoachApp;if(!app)return;

  app.calculatorMarkup=function(prefix){
    const id=app.esc(prefix||'math-question');
    return `<div class="card practice-calculator-card math-modal-calculator" data-study-calculator><div class="practice-calculator-head"><h3>Calculator</h3><span class="status">Scratch tool</span></div><label class="practice-calculator-label" for="${id}-calculator-display">Calculation</label><input id="${id}-calculator-display" class="practice-calculator-display" data-calculator-display type="text" inputmode="decimal" autocomplete="off" spellcheck="false" placeholder="0" aria-describedby="${id}-calculator-status"><div id="${id}-calculator-status" class="practice-calculator-status" data-calculator-status aria-live="polite">Basic arithmetic · nothing is saved</div><div class="practice-calculator-keys" role="group" aria-label="Calculator keypad"><button type="button" class="calculator-key utility" data-calculator-action="clear">C</button><button type="button" class="calculator-key utility" data-calculator-action="backspace" aria-label="Backspace">⌫</button><button type="button" class="calculator-key utility" data-calculator-value="(">(</button><button type="button" class="calculator-key utility" data-calculator-value=")">)</button><button type="button" class="calculator-key" data-calculator-value="7">7</button><button type="button" class="calculator-key" data-calculator-value="8">8</button><button type="button" class="calculator-key" data-calculator-value="9">9</button><button type="button" class="calculator-key operator" data-calculator-value="/">÷</button><button type="button" class="calculator-key" data-calculator-value="4">4</button><button type="button" class="calculator-key" data-calculator-value="5">5</button><button type="button" class="calculator-key" data-calculator-value="6">6</button><button type="button" class="calculator-key operator" data-calculator-value="*">×</button><button type="button" class="calculator-key" data-calculator-value="1">1</button><button type="button" class="calculator-key" data-calculator-value="2">2</button><button type="button" class="calculator-key" data-calculator-value="3">3</button><button type="button" class="calculator-key operator" data-calculator-value="-">−</button><button type="button" class="calculator-key" data-calculator-value="0">0</button><button type="button" class="calculator-key" data-calculator-value=".">.</button><button type="button" class="calculator-key" data-calculator-value="%">%</button><button type="button" class="calculator-key operator" data-calculator-value="+">+</button><button type="button" class="calculator-key equals" data-calculator-action="calculate">=</button></div></div>`;
  };

  app.whiteboardMarkup=function(skill,question,response,showAnswer){
    if(!question)return'';
    const open=app.state.whiteboardOpen;const pinned=app.state.whiteboardPinned;
    const recheck=response&&!response.correct?response.message:null;
    return `<section class="math-whiteboard ${pinned?'pinned':''}" data-math-whiteboard ${open?'':'hidden'} aria-live="polite"><header><div><span class="math-whiteboard-kicker">Coach Bob whiteboard</span><h3>Work the pathway, not just the arithmetic</h3></div><div class="math-whiteboard-controls"><button class="btn secondary" type="button" data-math-whiteboard-pin aria-pressed="${pinned?'true':'false'}">${pinned?'📌 Pinned':'Pin board'}</button><button class="icon-close" type="button" data-math-whiteboard-close aria-label="Close whiteboard">×</button></div></header><div class="math-whiteboard-step"><span>1 · What is the problem asking?</span><strong>${app.esc(question.prompt)}</strong><small>Answer unit: ${app.esc(question.unit||skill.unit)}</small></div><div class="math-whiteboard-step"><span>2 · Put the formula on the board</span><code>${app.esc(skill.formula)}</code>${skill.alternateFormula?`<small>Alternate setup: ${app.esc(skill.alternateFormula)}</small>`:''}</div><div class="math-whiteboard-step"><span>3 · Set up before calculating</span><p>${app.esc(question.hint||skill.memoryClue||skill.coachBobNote)}</p></div>${recheck?`<div class="math-whiteboard-step recheck"><span>4 · Recheck this part</span><p>${app.esc(recheck)}</p></div>`:''}${showAnswer&&response?`<div class="math-whiteboard-answer"><span>Expected result</span><strong>${app.esc(response.answer)} ${app.esc(response.unit)}</strong></div>`:''}<p class="math-whiteboard-stay">This board stays open while you retry. It does not disappear on a timer.</p></section>`;
  };

  app.guidedQuestion=(skill,record)=>{
    const held=app.state.guidedQuestionId&&(skill.guided||[]).find(question=>String(question.id)===String(app.state.guidedQuestionId));
    if(held)return held;
    const question=skill.guided[Math.min(record.guidedAttempts,skill.guided.length-1)]||skill.guided[0];
    if(question)app.state.guidedQuestionId=question.id;
    return question;
  };

  app.renderGuided=function(skill,record){
    const question=app.guidedQuestion(skill,record);const feedback=app.state.guidedFeedback;const sameFeedback=feedback&&String(feedback.questionId)===String(question.id)?feedback:null;const previousValue=sameFeedback&&!sameFeedback.correct&&sameFeedback.value!=null?String(sameFeedback.value):'';
    app.workspace.innerHTML=`<div class="math-question-modal-frame">${app.stageHeader(skill,record,'Try it with help')}<div class="math-question-modal-grid"><div class="math-question-column"><article class="card math-practice-panel"><span class="math-question-label">Guided question</span><h3>${app.esc(question.prompt)}</h3><form data-math-guided-form><label class="math-answer-field"><span>Your answer</span><div><input type="number" step="any" inputmode="decimal" name="answer" value="${app.esc(previousValue)}" aria-describedby="math-guided-unit" data-math-modal-focus><strong id="math-guided-unit">${app.esc(question.unit)}</strong></div></label><div class="math-inline-actions"><button class="btn secondary" type="button" data-math-hint aria-expanded="${app.state.whiteboardOpen?'true':'false'}">${app.state.whiteboardOpen?'Hide whiteboard help':'Open whiteboard help'}</button><button class="btn primary" type="submit">Check my work</button></div></form>${sameFeedback?`<div class="math-feedback ${sameFeedback.correct?'correct':'retry'}" role="status"><strong>${sameFeedback.correct?'That pathway works.':'Recheck this step.'}</strong><p>${app.esc(sameFeedback.message)}</p>${!sameFeedback.correct?'<p><strong>Your problem will stay here while you review the whiteboard and retry.</strong></p>':''}</div>`:''}</article><div class="math-stage-actions"><button class="btn secondary" type="button" data-math-stage="worked">Review worked example</button></div></div><aside class="math-question-toolrail" aria-label="Math question tools">${app.calculatorMarkup('guided')}${app.whiteboardMarkup(skill,question,sameFeedback,false)}</aside></div></div>`;
  };

  app.answerForm=function(questions,kind){return `<form data-math-set-form="${app.esc(kind)}"><div class="math-question-list">${questions.map((question,index)=>`<fieldset class="math-set-question"><legend><span>${index+1}</span>${app.esc(question.prompt)}</legend><label><span>Answer</span><div><input type="number" step="any" inputmode="decimal" name="${app.esc(question.id)}" ${index===0?'data-math-modal-focus':''}><strong>${app.esc(question.unit)}</strong></div></label>${question.hint?`<details><summary>Coach Bob clue</summary><p>${app.esc(question.hint)}</p></details>`:''}</fieldset>`).join('')}</div><button class="btn primary" type="submit">${kind==='mastery'?'Score mastery check':'Check independent practice'}</button></form>`;};

  app.setReview=function(skill,feedback,kind){
    if(!feedback)return'';
    return `<section class="math-set-result ${feedback.passed?'pass':'retry'}" role="status"><h3>${feedback.heading}</h3><strong>${feedback.correct}/${feedback.total} correct · ${feedback.percent}%</strong><p>${feedback.message}</p></section><div class="math-answer-review">${feedback.responses.map((response,index)=>`<details class="${response.correct?'correct':'retry'}" ${!response.correct&&String(response.id)===String(app.state.whiteboardQuestionId)?'open':''}><summary>${index+1}. ${response.correct?'Correct':'Review'} · ${app.esc(response.unit)}</summary><p><strong>Expected:</strong> ${app.esc(response.answer)} ${app.esc(response.unit)}</p>${response.correct?'':`<p>${app.esc(response.message)}</p><button class="btn secondary math-pin-explanation" type="button" data-math-whiteboard-question="${app.esc(response.id)}" data-math-whiteboard-kind="${app.esc(kind)}">📌 Pin explanation</button>`}</details>`).join('')}</div>`;
  };

  app.reviewWhiteboard=function(skill,feedback){
    if(!feedback||!app.state.whiteboardQuestionId)return'';
    const question=app.questionById(skill,app.state.whiteboardQuestionId);const response=(feedback.responses||[]).find(item=>String(item.id)===String(app.state.whiteboardQuestionId));
    return app.whiteboardMarkup(skill,question,response,true);
  };

  app.renderIndependent=function(skill,record){
    const feedback=app.state.setFeedback;
    app.workspace.innerHTML=`<div class="math-question-modal-frame">${app.stageHeader(skill,record,'Independent practice')}<div class="math-question-modal-grid"><div class="math-question-column"><article class="card"><h3>Work these without step-by-step help</h3><p>Coach Bob clues remain available, but set up each formula independently before opening them.</p>${app.answerForm(skill.independent,'independent')}${app.setReview(skill,feedback,'independent')}</article><div class="math-stage-actions"><button class="btn secondary" type="button" data-math-stage="learn">Review this skill</button>${feedback&&feedback.passed?'<button class="btn primary" type="button" data-math-continue-mastery>Continue to mastery check</button>':''}</div></div><aside class="math-question-toolrail" aria-label="Math question tools">${app.calculatorMarkup('independent')}${app.reviewWhiteboard(skill,feedback)}</aside></div></div>`;
  };

  app.renderMastery=function(skill,record){
    const feedback=app.state.setFeedback;
    app.workspace.innerHTML=`<div class="math-question-modal-frame">${app.stageHeader(skill,record,'Mastery check')}<div class="math-question-modal-grid"><div class="math-question-column"><article class="card"><h3>Complete the mastery check</h3><p>Score ${Number(app.state.catalog.passPercent||app.engine.PASS_PERCENT)}% or higher to earn the ${app.esc(skill.shortTitle)} mastery medal.</p>${app.answerForm(skill.mastery,'mastery')}${app.setReview(skill,feedback,'mastery')}</article><div class="math-stage-actions"><button class="btn secondary" type="button" data-math-stage="learn">Review this skill</button></div></div><aside class="math-question-toolrail" aria-label="Math question tools">${app.calculatorMarkup('mastery')}${app.reviewWhiteboard(skill,feedback)}</aside></div></div>`;
  };

  app.renderComplete=function(skill,record){
    const next=app.engine.nextSkillId(app.state.catalog,skill.id);
    app.workspace.innerHTML=`${app.stageHeader(skill,record,'Skill mastered')}<article class="math-complete-card"><div class="math-medal" aria-hidden="true">🏅</div><span class="status green">Mastered</span><h2>${app.esc(skill.title)}</h2><p>You earned this skill medal by scoring ${record.bestMasteryPercent}% on the mastery check.</p><small>Earned ${app.formatDate(record.masteredAt)}</small></article><div class="math-complete-actions"><button class="btn secondary" type="button" data-math-practice-again>Practice again</button><button class="btn secondary" type="button" data-math-review-skill>Review this skill</button>${next?`<button class="btn primary" type="button" data-math-next-skill="${app.esc(next)}">Continue to the next skill</button>`:`<button class="btn primary" type="button" data-math-exit>Return to Math Coach skills</button>`}</div>`;
  };

  app.renderWorkspace=function(){
    const skill=app.state.currentSkill&&app.skillById(app.state.currentSkill);if(!skill){app.workspace.hidden=true;app.workspace.innerHTML='';document.body.classList.remove('math-question-modal-open');return;}
    const storedRecord=app.recordFor(skill.id);const displayStage=app.state.questionReviewKind==='independent'&&app.state.setFeedback?'independent':storedRecord.stage;const record=displayStage===storedRecord.stage?storedRecord:{...storedRecord,stage:displayStage};const questionStage=['guided','independent','mastery'].includes(displayStage);
    app.workspace.hidden=false;app.workspace.setAttribute('data-stage',displayStage);
    if(questionStage){app.workspace.dataset.questionModal='true';app.workspace.setAttribute('role','dialog');app.workspace.setAttribute('aria-modal','true');app.workspace.setAttribute('aria-label',`${app.stageLabel(displayStage)} · ${skill.title}`);app.workspace.tabIndex=-1;document.body.classList.add('math-question-modal-open');}
    else{delete app.workspace.dataset.questionModal;app.workspace.removeAttribute('role');app.workspace.removeAttribute('aria-modal');app.workspace.removeAttribute('aria-label');app.workspace.removeAttribute('tabindex');document.body.classList.remove('math-question-modal-open');}
    if(displayStage==='worked')app.renderWorked(skill,record);else if(displayStage==='guided')app.renderGuided(skill,record);else if(displayStage==='independent')app.renderIndependent(skill,record);else if(displayStage==='mastery')app.renderMastery(skill,record);else if(displayStage==='complete')app.renderComplete(skill,record);else app.renderLearn(skill,record);
    if(root.RPSGTStudyCalculator)root.RPSGTStudyCalculator.init(app.workspace);
    if(questionStage){requestAnimationFrame(()=>{const target=app.workspace.querySelector('[data-math-modal-focus]')||app.workspace;if(target&&typeof target.focus==='function')target.focus({preventScroll:true});});}
    else app.workspace.scrollIntoView({behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'start'});
  };
  app.refresh=function(){app.renderSummary();app.renderCatalog();app.renderWorkspace();};
})(window);
